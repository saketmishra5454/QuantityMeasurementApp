# Database Configuration
spring.datasource.url=jdbc:postgresql://db.catvrmghbqkmjcztkaij.supabase.co:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=${SUPABASE_DB_PASSWORD}

# Driver
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.open-in-view=false

# JWT Configuration
app.jwt.secret=QuantoraQuantityMeasurementJwtSecretKey2026Secure
app.jwt.expiration-ms=3600000

# CORS Configuration
app.cors.allowed-origins=http://127.0.0.1:4200,http://localhost:4200,http://127.0.0.1:4300,http://localhost:4300,http://127.0.0.1:5173,http://localhost:5173,https://quantity-measurement-app-frontend-hijc-45gz25esj.vercel.app

